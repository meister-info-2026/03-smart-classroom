--extra-index-url https://download.pytorch.org/whl/cpu

# ==============================================================================
# [스마트 교실 비전 AI 클라이언트 (vision) 의존성 목록]
#
# 💡 [학생 필독 - 2차 고도화 진행 시 시행착오 방지 가이드]
# 1. 권장 파이썬 버전 및 가상환경 생성 (핵심!):
#    - 반드시 Python 3.12로 생성해야 합니다 (3.14 등은 PyTorch/ONNX DLL 로드 에러 발생).
#    - .gitignore 등록 규격에 맞춰 폴더명을 반드시 'venv'로 생성하세요:
#      powershell> py -3.12 -m venv venv
#      powershell> .\venv\Scripts\Activate.ps1
#      powershell> pip install -r requirements.txt
#
# 2. 얼굴인식 구현 방식 (인식률 및 설치 안정성):
#    - dlib, face_recognition, insightface(소스)는 Windows에서 C++ 컴파일러(Visual Studio
#      Build Tools) 미설치 시 심각한 빌드 에러를 유발합니다.
#    - 본 프로젝트에서는 설치 실패율 0%이면서 LFW 99.5% 이상의 최상급 인식률과
#      CPU 실시간 추론(15ms/30+ FPS)을 보장하는 경량 ONNX/OpenCV DNN 방식을 사용합니다:
#      ① ONNXRuntime: ArcFace / MobileFaceNet 경량 ONNX 모델 (LFW 99.5%+, 코사인 유사도 판정)
#      ② OpenCV 내장 DNN: YuNet 얼굴 검출 + SFace 얼굴 인식 (cv2.FaceRecognizerSF)
#      ③ MediaPipe Face Detection: 초경량 얼굴/랜드마크 감지 (3.10~3.12 최적)
#
# 3. Windows 필수 런타임:
#    - torch/opencv DLL 오류([WinError 1114]) 발생 시 'Visual C++ 2015-2022 x64' 설치 필요.
# 4. Windows 긴 경로 제한 오류 ([WinError 206]):
#    - 관리자 PowerShell: New-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Control\FileSystem" -Name "LongPathsEnabled" -Value 1 -PropertyType DWORD -Force
# ==============================================================================

# ------------------------------------------------------------------------------
# 1. 딥러닝 & 영상 처리 기본 엔진 (파이썬 버전별 자동 분기)
# ------------------------------------------------------------------------------
# Python 3.10 ~ 3.12 환경 (권장: 충돌 없는 검증된 안정 버전)
torch==2.2.2+cpu; python_version < "3.13"
torchvision==0.17.2+cpu; python_version < "3.13"
numpy<2; python_version < "3.13"
opencv-python==4.9.0.80; python_version < "3.13"

# Python 3.13 이상 환경 (신형 파이썬 설치자 자동 대응)
torch>=2.5.0; python_version >= "3.13"
torchvision>=0.20.0; python_version >= "3.13"
opencv-python>=4.10.0; python_version >= "3.13"

# ------------------------------------------------------------------------------
# 2. YOLOv8 객체 감지 (사람 검출 및 6석 좌석 ROI 착석/재실 모니터링)
# ------------------------------------------------------------------------------
ultralytics>=8.0.0

# ------------------------------------------------------------------------------
# 3. 얼굴 검출 & 랜드마크 (3.10~3.12 권장)
# ------------------------------------------------------------------------------
mediapipe; python_version < "3.13"

# ------------------------------------------------------------------------------
# 4. 얼굴 인식 & 경량 ONNX 모델 추론 엔진
# (ArcFace, MobileFaceNet, SFace 등 ONNX 모델을 C++ 빌드 도구 없이 즉시 실행)
# ------------------------------------------------------------------------------
onnxruntime

# ------------------------------------------------------------------------------
# 5. 얼굴 특징 벡터 코사인 유사도(Cosine Similarity) 및 거리 계산
# ------------------------------------------------------------------------------
scipy

# ------------------------------------------------------------------------------
# 6. 웹캠 화면 한글 UI 오버레이 (학생 이름/학번/출석 상태 한글 표시)
# (cv2.putText는 한글이 깨지므로 PIL ImageDraw + 폰트 렌더링에 필수)
# ------------------------------------------------------------------------------
pillow>=10.0.0

# ------------------------------------------------------------------------------
# 7. 백엔드 통신 (POST /api/v1/vision/events) & 환경 변수(.env) 로드
# ------------------------------------------------------------------------------
requests>=2.28.0
python-dotenv>=1.0.0
